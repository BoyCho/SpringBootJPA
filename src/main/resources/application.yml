
spring:
  datasource:
    url: jdbc:mysql://3.129.240.89:3306/example?serverTimezone=UTC&characterEncoding=UTF-8
    username: HJ
    password: ****
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace